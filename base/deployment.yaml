apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
  namespace: develop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - image: diegoruizrei/infra-test:0.0.14
        name: demo
        ports:
        - containerPort: 8080